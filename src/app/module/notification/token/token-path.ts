import { NOTIFICATION_MODULE_BASE_ROUTE } from '../module-route-name.ts';

const TOKEN_BASE = 'token';
const TOKEN_BASE_FULL = NOTIFICATION_MODULE_BASE_ROUTE + '/' + TOKEN_BASE;

export const TOKEN_PATH_FULL = {
  INFO: TOKEN_BASE_FULL + '/info',
};
